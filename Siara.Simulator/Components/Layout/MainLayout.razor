@inherits LayoutComponentBase
@inject AuthenticationService AuthService
@inject NavigationManager NavManager

<div class="page">
    <header class="app-header">
        <div class="logo-container">
            <img src="/logo-siara.jpg" alt="SIARA Logo" style="height: 50px; width: auto; object-fit: contain;" />
            <h1>Sistema de Atención de Requerimientos de Autoridad (SIARA)</h1>
        </div>
        <div class="user-controls">
            @if (AuthService.IsAuthenticated)
            {
                <button class="btn btn-sm btn-outline-light" @onclick="HandleLogout">Cerrar Sesión (Logout)</button>
            }
        </div>
    </header>

    <main class="app-content">
        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

@code {
    private void HandleLogout()
    {
        AuthService.Logout();
        NavManager.NavigateTo("/login", forceLoad: true);
    }
}